import { CardCreateService } from "@/app/(privates)/(routes)/servicebook/service/createservice/components/CardCreateService";

export default function CreateService() {
  return <CardCreateService />;
}
